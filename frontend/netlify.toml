[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

# Proxy all /api requests to the backend
[[redirects]]
  from = "/api/*"
  to = "https://stock-prediction-api.onrender.com/api/:splat"
  status = 200
